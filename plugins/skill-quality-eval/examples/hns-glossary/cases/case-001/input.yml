# Case input for hns-glossary baseline / run
#
# The skill-quality-eval runner reads this YAML verbatim and includes it in
# the invoke prompt. **DO NOT put expected outputs / answer hints in this file**
# — anything written here is seen by the LLM and can leak the ground truth
# (this was a real dogfooding incident: a `description` field listing expected
# terms caused the model to paraphrase the description instead of analyzing
# the fixture).
#
# Keep this file to bare instruction:  what skill / what mode / what fixture
# path / what args. Documentation for humans goes in a sibling _notes.md.

skill_id: hns:glossary
mode: shallow
fixture: ../../fixtures/toy-domain
args: ["--shallow", "{{FIXTURE}}"]
