---
description: "Deprecated — use /hns:validate --docs instead"
---

# /hns:doc-validate (Deprecated)

> **이 커맨드는 `/hns:validate`로 통합되었습니다.**
>
> - `/hns:validate --docs` — 문서 일관성 검증 (기존 doc-validate 기능)
> - `/hns:validate --code` — 코드 규칙 준수 검증 (신규)
> - `/hns:validate` — 둘 다 실행
