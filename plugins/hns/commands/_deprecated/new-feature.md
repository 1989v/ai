---
description: "Deprecated — use /hns:feat instead"
---

# /hns:new-feature (Deprecated)

> **이 커맨드는 `/hns:feat`로 통합되었습니다.**
>
> - `/hns:feat` — 전체 파이프라인 (shape → write → review → tasks → implement → validate)
> - `/hns:feat --no-validate` — 검증 단계 스킵
> - `/hns:feat --skip-to implement` — 구현부터 시작
