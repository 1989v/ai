# Step {{N}}: {{NAME}}

> 이 파일은 **독립된 subagent 컨텍스트**에서 실행된다. "이전 대화에서 논의한 대로" 같은
> 외부 참조는 금지한다. 필요한 정보는 전부 이 파일 안에 적는다.

## 읽어야 할 파일

먼저 아래 파일들을 읽고 아키텍처와 설계 의도를 파악하라:

- `docs/architecture/overview.md`
- `docs/specs/{{SPEC}}/spec.md`
- {{이전 step에서 생성/수정된 파일 경로}}

이전 step에서 만들어진 코드를 꼼꼼히 읽고, 설계 의도를 이해한 뒤 작업하라.

## 작업

{{구체적 구현 지시. 파일 경로, 클래스/함수 시그니처, 로직 설명.
코드 스니펫은 인터페이스/시그니처 수준만 제시하고, 구현체는 에이전트에게 맡겨라.
단, 설계 의도에서 벗어나면 안 되는 핵심 규칙(멱등성·보안·데이터 무결성 등)은 명확히 박아넣어라.}}

## Acceptance Criteria

> 추상 서술("~가 동작해야 한다") 금지. **실행 가능한 검증 커맨드**로 적는다.
> 언어/스택에 맞게 교체 — 아래는 예시일 뿐이다.

```bash
{{빌드 커맨드}}   # 컴파일 에러 없음   (예: ./gradlew build / npm run build / cargo build)
{{테스트 커맨드}} # 테스트 통과         (예: ./gradlew test  / npm test       / cargo test)
```

## 검증 절차

1. 위 AC 커맨드를 실행한다.
2. 가드레일 체크리스트를 확인한다:
   - architecture 문서의 디렉토리 구조/의존성 방향을 따르는가?
   - CLAUDE.md의 CRITICAL 규칙을 위반하지 않았는가?
   - spec.md의 범위를 벗어나거나 누락하지 않았는가?
3. 결과에 따라 `steps/index.json`의 해당 step을 업데이트한다:
   - 성공 → `"status": "completed"`, `"summary": "산출물 한 줄 요약"`
   - 수정 3회 후에도 실패 → `"status": "error"`, `"error_message": "구체적 에러"`
   - 사용자 개입 필요(API키·외부인증·수동설정) → `"status": "blocked"`, `"blocked_reason": "사유"` 후 즉시 중단

## 금지사항

- {{이 step에서 하지 말아야 할 것. "X를 하지 마라. 이유: Y" 형식}}
- 기존 테스트를 깨뜨리지 마라.
- 이 step에 명시되지 않은 추가 기능·파일을 만들지 마라.
