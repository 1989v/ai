---
name: io-setup
description: "Set up IO capture in the target service for debugging"
---

# /io-setup

Use the `ai-debugger:io-interceptor` skill to handle this request.

Follow that skill exactly:
- analyze the target project and framework
- install or generate the required IO capture components
- align package names and placement with the project's conventions
- verify the integration path described by the skill
