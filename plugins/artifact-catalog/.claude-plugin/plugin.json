{
  "name": "artifact-catalog",
  "description": "Keep a per-vault register of every Claude artifact the account has published and publish it as a searchable, project-grouped catalog page per vault. A PostToolUse hook nudges the session to sync after every publish, so new artifacts keep flowing in. Registry survives the 50-item Artifact list window; company and personal never share a page.",
  "version": "0.2.0",
  "author": {
    "name": "gideok-kwon"
  },
  "license": "MIT",
  "keywords": [
    "claude-artifact",
    "catalog",
    "registry",
    "obsidian",
    "publishing"
  ]
}
